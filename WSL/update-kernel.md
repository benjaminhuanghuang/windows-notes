http://www.linuxeden.com/a/108904

```
sudo add-apt-repository ppa:cappelikan/ppa
sudo apt update
sudo apt -y install mainline

mainline --list

sudo mainline --install-latest

sudo mainline --install <version>
```